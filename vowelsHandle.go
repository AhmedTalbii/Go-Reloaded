package goreloded

// import "strings"

// func isVowel(str string) bool {

// }

func VowelsHandle(str string) string {
	// arr := strings.Fields(str)
	// for i, w := range arr {
	// 	if w == "a" || w == "A" &&  {

	// 	}
	// }
	return str
}